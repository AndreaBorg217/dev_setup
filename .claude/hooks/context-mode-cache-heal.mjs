#!/usr/bin/env node
// Symlinked via tasks/claude.yml:84 → ~/.claude/hooks/context-mode-cache-heal.mjs
// SessionStart heal — no-op stub when context-mode plugin not installed.
// Plugin provides real hooks via ${CLAUDE_PLUGIN_ROOT} when installed.
process.exit(0);
