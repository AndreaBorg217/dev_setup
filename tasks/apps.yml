- name: Install apps
  community.general.homebrew_cask:
    name: "{{ item }}"
    state: present
  loop:
    - notion
    - dropbox
    - google-chrome
    - spotify
    - brave-browser
    - bruno
    - bitwarden
    - coderabbit
  tags:
    - "apps"
